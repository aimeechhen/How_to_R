


# install python via microsoft store (via python website or python.exe caused issues/was not detected?)

# check version
python --version
pip --version

# install pip3 via git bash through python
python -m pip



library(reticulate)